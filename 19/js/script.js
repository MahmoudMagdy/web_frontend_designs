$(document).ready(() => {

    /*const articleContent = $('#article-content');
    $('#article-image').height(articleContent.height());
    articleContent.resize(() => {
        $('#article-image').height(this.height());
    });*/
});